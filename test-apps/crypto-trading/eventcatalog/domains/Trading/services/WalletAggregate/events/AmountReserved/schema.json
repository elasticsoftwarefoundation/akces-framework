{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AmountReserved",
  "type": "object",
  "properties": {
    "userId": {
      "type": "string",
      "description": "The user ID that owns the wallet (aggregate identifier)."
    },
    "currency": {
      "type": "string",
      "description": "The type of cryptocurrency being reserved (e.g., \"BTC\", \"ETH\")."
    },
    "amount": {
      "type": "string",
      "description": "The amount of currency being reserved (represented as a string to preserve decimal precision)."
    },
    "referenceId": {
      "type": "string",
      "description": "The unique identifier for the reservation, typically referencing an order or transaction ID."
    }
  },
  "required": ["userId", "currency", "amount", "referenceId"],
  "additionalProperties": false
}